export type ProductProps = {
    id: string;
      name: string;
      images:string[]
      description: string,
      price: number,
      slug: string,
      createdAt: string,
      updatedAt: string
}